# Copy this file to .env and fill in your key. .env is gitignored.
# Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Optional: enables the interviewer's GitHub portfolio tools (MCP).
# Create a fine-grained PAT with read-only access to public repositories at
# https://github.com/settings/personal-access-tokens
GITHUB_PAT=

# Sign-in and the authorized-account allowlist are NOT configured here: they
# live in .streamlit/secrets.toml ([auth] and [roles]). See
# .streamlit/secrets.toml.example and the README's "Sign-in and access"
# section. The app serves nobody until that file exists.
